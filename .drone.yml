pipeline:

  build_wordpress:
    image: plugins/docker:1.12
    username: ${DOCKER_USER}
    password: ${DOCKER_PASS}
    repo: leejelley/wordpress
    tags: 
       - latest
    dockerfile: wordpress/Dockerfile

  build_nginx:
    image: plugins/docker:1.12
    username: ${DOCKER_USER}
    password: ${DOCKER_PASS}
    repo: leejelley/ngix
    tags:
       - latest
    dockerfile: nginx/Dockerfile
